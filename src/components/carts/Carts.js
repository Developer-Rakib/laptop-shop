import React from 'react';

const Carts = () => {
    return (
        <div>
            <h4>Selected laptops</h4>
            <button>Chose for Me</button>
            <button>Reset</button>
        </div>
    );
};

export default Carts;